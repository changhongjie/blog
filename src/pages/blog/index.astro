---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { format } from 'date-fns';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const posts = (await getCollection('blog'))
	.filter((post) => import.meta.env.DEV || !post.data.draft)
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<Layout title="Blog - HJ Blog" description="Read my latest thoughts on Data Science, ML, and my hobbies.">
	<div class="space-y-8">
		<div class="space-y-2">
			<h1 class="text-4xl font-bold tracking-tight">Blog</h1>
			<p class="text-muted-foreground">
				Writing about data science, machine learning, and my travels.
			</p>
		</div>
		<div class="grid gap-6 md:grid-cols-2">
			{posts.map((post) => (
				<a href={`/blog/${post.id}`} class="group block">
					<Card class="overflow-hidden border border-muted-foreground/10 bg-muted/10 transition-all hover:bg-muted/30">
						<CardHeader class="px-6 py-6">
							<div class="flex items-center gap-2 mb-2">
								<span class="text-xs font-medium uppercase text-muted-foreground bg-muted/50 px-2 py-0.5 rounded">
									{post.data.category}
								</span>
								<span class="text-xs text-muted-foreground">•</span>
								<time class="text-xs text-muted-foreground">
									{format(post.data.pubDate, 'MMMM d, yyyy')}
								</time>
								{post.data.draft && (
									<span class="ml-auto rounded-full bg-yellow-100 px-2 py-0.5 text-[10px] font-medium text-yellow-800">
										Draft
									</span>
								)}
							</div>
							<CardTitle class="line-clamp-2 text-xl leading-tight font-bold">
								{post.data.title}
							</CardTitle>
							<CardDescription class="line-clamp-2 mt-2">
								{post.data.description}
							</CardDescription>
						</CardHeader>
					</Card>
				</a>
			))}
		</div>
	</div>
</Layout>

